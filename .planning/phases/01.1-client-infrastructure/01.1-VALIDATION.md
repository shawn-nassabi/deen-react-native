---
phase: 1.1
slug: client-infrastructure
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-11
---

# Phase 1.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None — no test runner installed (CLAUDE.md: "No test framework is configured") |
| **Config file** | None |
| **Quick run command** | `npx tsc --noEmit && npm run lint` |
| **Full suite command** | `npm run ios` (manual smoke test on iOS simulator) |
| **Estimated runtime** | ~30s (compile+lint), ~2–3 min (simulator launch) |

---

## Sampling Rate

- **After every task commit:** Run `npx tsc --noEmit && npm run lint`
- **After every plan wave:** Run `npm run ios` (manual smoke — verify no white screen, no Metro errors)
- **Before `/gsd:verify-work`:** Full suite (compile + lint + simulator launch) must pass
- **Max feedback latency:** ~30s for static checks; ~3 min for full smoke

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| polyfill-install | 01 | 1 | MIG-01 | compile | `npx tsc --noEmit` | ❌ W0 | ⬜ pending |
| polyfill-import | 01 | 1 | MIG-01 | compile+smoke | `npx tsc --noEmit` + `npm run ios` | ✅ (utils/polyfills.ts) | ⬜ pending |
| supabase-install | 01 | 1 | MIG-02 | compile | `npx tsc --noEmit` | ❌ W0 | ⬜ pending |
| supabase-client | 01 | 2 | MIG-02 | compile+smoke | `npx tsc --noEmit` + `npm run ios` | ❌ W0 | ⬜ pending |
| config-update | 01 | 2 | MIG-10 | compile | `npx tsc --noEmit` | ✅ (utils/config.ts) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

None — no test files to create. Project has no test infrastructure. CLAUDE.md explicitly states no tests exist.

*Existing infrastructure covers all phase requirements via TypeScript compilation and manual smoke test.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| App launches without `ReferenceError: URL is not defined` crash | MIG-01 | No automated test runner | Run `npm run ios`, watch Metro logs; must reach login screen without white screen or ReferenceError |
| `import { supabase } from "@/utils/supabase"` does not throw at module init | MIG-02 | No automated test runner | Run `npm run ios`; verify no white screen or Metro crash on launch |
| Existing Cognito auth flow still works end-to-end | Phase success criterion 4 | Requires device + live auth | Sign in via Cognito hosted UI; verify tokens returned and chat/hikmah screens load |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s (static) / 3min (smoke)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending

# Phase 1.4: Cleanup + Account - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-13
**Phase:** 1.4-cleanup-account
**Areas discussed:** expo-web-browser removal, Account deletion scope, AuthUser.sub alias cleanup

---

## expo-web-browser removal

| Option | Description | Selected |
|--------|-------------|----------|
| Replace with Linking.openURL | Swap openBrowserAsync for React Native's built-in Linking.openURL — no new dependency, fully satisfies MIG-09, opens system browser | ✓ |
| Keep expo-web-browser | Only remove expo-auth-session; leave expo-web-browser for ExternalLink. MIG-09 partially satisfied. | |
| Keep but mark intentional | Same as keeping, but add comment documenting the deliberate deviation. | |

**User's choice:** Replace with Linking.openURL
**Notes:** Clean removal — both expo-auth-session AND expo-web-browser fully removed after fixing ExternalLink.

---

## Account deletion scope

| Option | Description | Selected |
|--------|-------------|----------|
| Full flow: button + confirm + sign out | Build end-to-end: deleteAccount() in api.ts + Delete Account button in settings + Alert confirmation + sign out | ✓ |
| API function only, no UI | Add deleteAccount() to api.ts, verify it works with Supabase token, but no button in settings.tsx | |

**User's choice:** Full flow
**Notes:** ACC-01 interpreted as requiring a working, user-accessible flow — not just a tested API call.

| Placement option | Description | Selected |
|--------|-------------|----------|
| Bottom of settings, below sign out | Danger zone at bottom, visually separated, red/destructive styling | ✓ |
| Inside the Account section | Within existing Account card alongside user info | |

**User's choice:** Bottom of settings, below sign out

| Confirmation style | Description | Selected |
|--------|-------------|----------|
| Native Alert | Alert.alert() with Cancel / Delete buttons — platform-native, no extra component | ✓ |
| Inline confirmation | Card state swap (like forgot-password.tsx), stays in-app | |

**User's choice:** Native Alert

---

## AuthUser.sub alias cleanup

| Option | Description | Selected |
|--------|-------------|----------|
| Remove it | Delete sub?: string from AuthUser — TypeScript error catches any missed usages | ✓ |
| Keep it | Leave as permanent alias — safer but contradicts Phase 1.2 D-09 intent | |

**User's choice:** Remove it
**Notes:** Removal is the Phase 1.2 D-09 planned outcome. TypeScript errors treated as a safety feature.

---

## Claude's Discretion

- Exact destructive color for Delete Account button (check theme tokens first, fallback to #e53935)
- Loading state while delete API call is in flight
- Section separator style between Sign Out and Delete Account

## Deferred Ideas

- Password reset PKCE fix — CONCERNS.md tech debt, not Phase 1.4
- Dead code cleanup (api.ts unused functions, template files) — out of scope for auth migration

# Phase 1.3: Password Reset Flow - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-11
**Phase:** 01.3-password-reset-flow
**Areas discussed:** Forgot password entry, Post-email confirmation, Supabase reset mode

---

## Forgot password entry

| Option | Description | Selected |
|--------|-------------|----------|
| Separate screen | Navigate to app/forgot-password.tsx via router.push('/forgot-password'). Consistent with Phase 1.2 D-05 flat screen structure. Same elevated card layout. Clean back navigation. | ✓ |
| Inline within login card | Login card toggles state — email field replaces password field, button changes. No new route, no navigation. | |

**User's choice:** Separate screen (`app/forgot-password.tsx`)
**Notes:** Consistent with established flat screen structure from Phase 1.2 D-05.

---

## Post-email confirmation

| Option | Description | Selected |
|--------|-------------|----------|
| Inline 'Check your inbox' | Same screen transitions: input and button replaced by confirmation message within the card. No navigation. | ✓ |
| Navigate back to login | After submit, router.replace('/login') with a banner or status message on the login screen. | |

**User's choice:** Inline confirmation on same screen
**Notes:** Email address shown in confirmation message. "← Back to sign in" link remains.

---

## Supabase reset mode

| Option | Description | Selected |
|--------|-------------|----------|
| PKCE flow | Dashboard "Use PKCE flow" ON. Link carries ?code=... — app calls exchangeCodeForSession(). | ✓ |
| Implicit / legacy | Dashboard "Use PKCE flow" OFF. Link carries #access_token=...&type=recovery in URL hash. | |
| I'll check and report back | Unknown — implement PKCE as default, flag to verify. | |

**User's choice:** PKCE flow
**Notes:** Confirmed ON in Supabase dashboard.

**Follow-up — PKCE code exchange location:**

| Option | Description | Selected |
|--------|-------------|----------|
| Loading on same screen | New Password screen shows spinner while exchangeCodeForSession() runs, then reveals form. | ✓ |
| Separate 'processing' route | Transitional screen handles exchange, then navigates to form. | |

**User's choice:** Loading on the same screen (`app/reset-password.tsx`)

---

## Claude's Discretion

- Error message strings for `exchangeCodeForSession` failures (expired/already-used link)
- Handling of missing/invalid `code` param (graceful error state, not crash)
- Exact card padding, border radius, spacing (mirror login.tsx values)
- Loading spinner style on reset-password screen

## Deferred Ideas

None — discussion stayed within phase scope.

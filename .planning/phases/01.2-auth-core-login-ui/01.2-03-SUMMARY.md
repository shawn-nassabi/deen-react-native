---
phase: 01.2-auth-core-login-ui
plan: "03"
subsystem: auth-ui
tags: [signup, expo-router, supabase, react-native, form, auth-redirect]

# Dependency graph
requires:
  - phase: 01.2-01
    provides: hooks/useAuth.tsx with signUp(email, password) API
provides:
  - app/signup.tsx — Sign Up screen mirroring login.tsx card design; calls signUp via useAuth
  - app/_layout.tsx — signup Stack.Screen registered; auth redirect includes signup in exclusion check
affects:
  - app/login.tsx — signup screen is reachable from login via router.push('/signup')
  - all authenticated screens — _layout.tsx redirect logic now handles both auth screens

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "mapSignUpError() pure function — maps Supabase AuthError messages to friendly UI copy"
    - "router.canGoBack() guard before router.back() — prevents empty stack error on deep link entry"
    - "isOnAuthScreen replaces isOnLogin — single variable covers both auth routes"

key-files:
  created:
    - app/signup.tsx
  modified:
    - app/_layout.tsx
    - app/feedback.tsx

key-decisions:
  - "signup.tsx mirrors login.tsx card structure — same card, same inputs, different copy and signUp() call"
  - "router.canGoBack() ? router.back() : router.replace('/login') — safe back navigation from signup"
  - "isOnAuthScreen variable covers both 'login' and 'signup' segments — prevents redirect loop for signed-out users on /signup"

patterns-established:
  - "Error mapping function (mapSignUpError) extracts Supabase error messages to UI copy — pattern for future auth screens"
  - "ScrollView wrapping on auth screens ensures keyboard avoidance on small devices"

requirements-completed:
  - AUTH-02
  - UI-02
  - UI-05
  - UI-06

# Metrics
duration: ~5min
completed: 2026-04-11
---

# Phase 1.2 Plan 03: Sign Up Screen + Layout Fix Summary

**Sign Up screen with elevated card design (mirrors login.tsx) + _layout.tsx auth redirect fixed to allow signed-out users on /signup**

## Performance

- **Duration:** ~5 min
- **Completed:** 2026-04-11
- **Tasks:** 2
- **Files created:** 1
- **Files modified:** 2

## Accomplishments

- Created `app/signup.tsx` as a close visual mirror of `login.tsx` — same elevated card, same input styles, same loading pattern
- Calls `signUp(email.trim(), password)` from `useAuth()` and navigates to `/(tabs)` on success
- Full error message mapping: email already registered, weak password, network error, and default fallback
- Show/hide password toggle using Ionicons `eye-outline` / `eye-off-outline`
- `router.canGoBack() ? router.back() : router.replace('/login')` safe back navigation
- No "Forgot password?" link on Sign Up screen (per spec)
- `ScrollView` wrapping for keyboard avoidance on small devices
- Updated `app/_layout.tsx`: added `<Stack.Screen name="signup" options={{ headerShown: false }} />` and renamed `isOnLogin` to `isOnAuthScreen` (includes both "login" and "signup")
- Fixed pre-existing unescaped entity lint errors in `app/feedback.tsx` (Rule 3 — blocked task verification)

## Task Commits

1. **Task 1: Create app/signup.tsx — Sign Up screen mirroring login.tsx** — `b9742aa` (feat)
2. **Task 2: Update app/_layout.tsx — register signup route and fix auth redirect** — `6d8b6a2` (feat)

## Files Created/Modified

- `app/signup.tsx` — 214 lines; new Sign Up screen with card layout, email/password inputs, show/hide toggle, error handling
- `app/_layout.tsx` — added signup Stack.Screen, renamed isOnLogin → isOnAuthScreen with signup included in check
- `app/feedback.tsx` — fixed 4 pre-existing unescaped entity lint errors (`We're`, `you've`, `it's`, `You'll`)

## Decisions Made

- `router.canGoBack() ? router.back() : router.replace('/login')` — handles both normal flow (back to login) and deep link entry (no stack history)
- `isOnAuthScreen` variable consolidates both auth screen checks — single change point if a third auth route is added later
- Error mapping via `mapSignUpError()` pure function — clean separation from component render logic

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed pre-existing lint errors in app/feedback.tsx**
- **Found during:** Task 1 verification (`npm run lint`)
- **Issue:** 4 unescaped entity errors (`'` in JSX text) in `app/feedback.tsx` prevented lint from exiting 0
- **Fix:** Replaced `'` with `&apos;` in 3 JSX text nodes
- **Files modified:** `app/feedback.tsx`
- **Commit:** Included in Task 1 commit `b9742aa`

**2. [Merge] Merged worktree-agent-ad292c76 to bring Plan 01 Supabase auth code**
- **Found during:** Setup — worktree branch was based on master before Plan 01 changes
- **Fix:** `git merge worktree-agent-ad292c76` to bring `utils/auth.ts`, `hooks/useAuth.tsx`, `utils/supabase.ts`, and Plan 01 code
- **Impact:** Required before Task 1 could be implemented (signUp function availability)

## Known Stubs

None — the signup screen is fully wired to `signUp()` from `useAuth()`. All form state and error messages are real, not placeholder.

## Self-Check: PASSED

- [x] `app/signup.tsx` exists at correct path
- [x] `app/_layout.tsx` contains `Stack.Screen name="signup"`
- [x] `app/_layout.tsx` contains `isOnAuthScreen` (no `isOnLogin`)
- [x] Commit `b9742aa` exists: `feat(01.2-03): create app/signup.tsx`
- [x] Commit `6d8b6a2` exists: `feat(01.2-03): update app/_layout.tsx`
- [x] `npm run lint` exits 0 errors

---
*Phase: 01.2-auth-core-login-ui*
*Completed: 2026-04-11*

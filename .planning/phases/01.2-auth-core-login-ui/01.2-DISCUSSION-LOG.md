# Phase 1.2: Auth Core + Login UI - Discussion Log

**Date:** 2026-04-11
**Format:** Gray area selection → per-area Q&A

---

## Areas Selected

All four presented areas were selected for discussion:
1. Login/Sign Up UI Style
2. Screen Routing Structure
3. `useAuth` signIn() Signature
4. `utils/auth.ts` Fate

---

## Area 1: Login/Sign Up UI Style

**Q: What visual style for the Sign In / Sign Up screens?**
Options: Elevated card (Recommended), Flat — fields on background, Full-screen gradient
**Selected:** Elevated card

**Q: Logo placement and link placement within the card layout?**
Options: Logo above card, links inside (Recommended) / Logo inside card, links below
**Selected:** Logo above card, links inside — links ('Forgot password?' + 'Sign up') are inside the card

**Q: Input field style inside the card?**
Options: Bordered box (Recommended), Underline / bottom border, Filled / no border
**Selected:** Bordered box — `colors.border` base, emerald (#5bc1a1) focus highlight

---

## Area 2: Screen Routing Structure

**Q: Where should auth screens live?**
Options: Flat — app/signup.tsx (Recommended) / Auth group — app/(auth)/
**Selected:** Flat — `app/signup.tsx` alongside `app/login.tsx`

**Q: How should navigation between Sign In and Sign Up work?**
Options: router.push/replace (Recommended) / Links only (no back stack)
**Selected:** `router.push('/signup')` from login; `router.back()` from signup

---

## Area 3: `useAuth` signIn() Signature

**Q: How should the hook's signIn() evolve for email+password?**
Options: `signIn(email, password)` on hook (Recommended) / Screens call Supabase directly
**Selected:** `signIn(email: string, password: string)` on the hook

**Q: Where should signUp() live?**
Options: `signUp(email, password)` on `useAuth` (Recommended) / Standalone util
**Selected:** `signUp(email: string, password: string)` on `useAuth`

**Q: How should AuthUser type evolve?**
Options: Add `id`, keep `sub` as alias (Recommended) / Replace with Supabase User type / Minimal: id and email only
**Selected:** Add `id: string`, keep `sub?: string` as alias set to `id` — Phase 1.4 removes `sub` references

---

## Area 4: `utils/auth.ts` Fate

**Q: What should happen to utils/auth.ts?**
Options: Thin Supabase wrappers (Recommended) / Delete now, inline into useAuth
**Selected:** Keep as thin wrappers — gut Cognito, replace with Supabase calls

**Q: Should getValidAccessToken() be preserved?**
Options: Preserve as wrapper (Recommended) / api.ts imports supabase directly
**Selected:** Preserve `getValidAccessToken()` as thin wrapper over `supabase.auth.getSession()` — api.ts import unchanged

---

*Log generated: 2026-04-11*

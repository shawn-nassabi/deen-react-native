---
phase: quick-260416-s86
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/hikmah/TreeCardSkeleton.tsx
  - components/hikmah/LessonRowSkeleton.tsx
  - app/(tabs)/hikmah.tsx
  - app/hikmah/[treeId].tsx
autonomous: false
requirements:
  - QUICK-260416-s86-01
  - QUICK-260416-s86-02

must_haves:
  truths:
    - "Hikmah tree list (app/(tabs)/hikmah.tsx) shows 3 animated skeleton cards instead of ActivityIndicator while loading"
    - "Hikmah tree detail ([treeId].tsx) shows hero skeleton + 4 lesson row skeletons instead of bare ActivityIndicator while loading"
    - "Skeleton shimmer uses the same Animated.loop + LinearGradient pattern as components/references/ReferenceSkeleton.tsx"
    - "Skeletons are theme-aware (different highlight alpha for light vs dark) via Colors[colorScheme]"
    - "Tree detail loading state sits below the blur header at the same headerPaddingTop offset used by the non-loading branch"
    - "Tree list loading caption 'Loading topics...' is preserved below the skeleton stack"
    - "`npm run lint` passes with zero new warnings"
  artifacts:
    - path: "components/hikmah/TreeCardSkeleton.tsx"
      provides: "Shimmer skeleton card mirroring TreeCard shape (panel bg, rounded 16, title + summary lines + tag/count row + View Tree button placeholder + progress bar)"
      min_lines: 120
    - path: "components/hikmah/LessonRowSkeleton.tsx"
      provides: "Shimmer skeleton row mirroring lesson row shape (circular index badge + title + est-minutes sub-line)"
      min_lines: 90
    - path: "app/(tabs)/hikmah.tsx"
      provides: "Loading branch renders 3 TreeCardSkeletons + 'Loading topics...' caption"
      contains: "TreeCardSkeleton"
    - path: "app/hikmah/[treeId].tsx"
      provides: "Loading branch renders hero skeleton + 4 LessonRowSkeletons below blur header"
      contains: "LessonRowSkeleton"
  key_links:
    - from: "components/hikmah/TreeCardSkeleton.tsx"
      to: "expo-linear-gradient + react-native Animated"
      via: "Animated.loop translating LinearGradient across the card"
      pattern: "Animated\\.loop"
    - from: "components/hikmah/LessonRowSkeleton.tsx"
      to: "expo-linear-gradient + react-native Animated"
      via: "Animated.loop translating LinearGradient across the row"
      pattern: "Animated\\.loop"
    - from: "app/(tabs)/hikmah.tsx"
      to: "components/hikmah/TreeCardSkeleton"
      via: "default import + 3 stacked instances in loading branch"
      pattern: "import TreeCardSkeleton"
    - from: "app/hikmah/[treeId].tsx"
      to: "components/hikmah/LessonRowSkeleton"
      via: "default import + 4 stacked instances in loading branch"
      pattern: "import LessonRowSkeleton"
---

<objective>
Replace the two Hikmah loading states (tree list + tree detail) with animated skeleton shimmer cards that mirror the final content shape, matching the treatment already applied to the References page.

Purpose: Visual polish — the current ActivityIndicator spinners feel jarring and make the app look less mature than the shimmer pattern already deployed on the References page. Shimmer skeletons give users a sense of structure and progress while content loads.

Output: Two new components (`TreeCardSkeleton`, `LessonRowSkeleton`), and both Hikmah loading branches rewired to use them with appropriate header-offset positioning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@components/references/ReferenceSkeleton.tsx
@components/references/ReferencesContainer.tsx
@components/hikmah/TreeCard.tsx
@app/(tabs)/hikmah.tsx
@app/hikmah/[treeId].tsx

<interfaces>
<!-- Canonical shimmer pattern from components/references/ReferenceSkeleton.tsx — mirror exactly. -->
<!-- Key shape: Animated.loop(Animated.timing(shimmerAnim, { toValue: 1, duration: 1400, easing: Easing.linear, useNativeDriver: true })) -->
<!-- Shimmer layer: absolute-positioned <Animated.View> with transform [{ translateX }] wrapping a <LinearGradient> -->
<!-- Card wrapper requires `overflow: "hidden"` so shimmer sweep is clipped -->
<!-- Theme-aware highlight: dark => "rgba(255,255,255,0.06)", light => "rgba(255,255,255,0.65)"; transparent color: "rgba(255,255,255,0)" -->
<!-- Shimmer width constant: 200; output range: [-SHIMMER_WIDTH, Dimensions.get("window").width] -->
<!-- All skeleton "block" placeholders use colors.hoverBg as backgroundColor and borderRadius: 4 -->

<!-- TreeCard shape (components/hikmah/TreeCard.tsx) — things the skeleton must approximate: -->
<!-- - card: borderRadius 16, borderWidth 1, backgroundColor colors.panel, borderColor colors.border, marginBottom 16 -->
<!-- - content padding: 20 horizontal, paddingBottom 40 (leaves room for progress bar) -->
<!-- - header: title block (fontSize 20, height ~24) + 2-3 summary lines (fontSize 14, lineHeight 20) -->
<!-- - metaRow: tag placeholders (2 small pills) on left + count placeholder on right -->
<!-- - actions: single full-width "View Tree" button placeholder (height ~44, borderRadius 12) -->
<!-- - progressContainer: absolute bottom, height 4, full width (use colors.border tone, no fill) -->

<!-- Lesson row shape (app/hikmah/[treeId].tsx sortedLessons.map) — things the row skeleton must approximate: -->
<!-- - lessonItem: borderRadius 16, padding 16, flexDirection row, borderWidth 1, borderColor colors.border, backgroundColor colors.panel -->
<!-- - lessonLeft: flexDirection row, gap 12, flex 1 -->
<!-- - indexBadge: circular 32x32, borderRadius 16, borderWidth 1.5 -->
<!-- - lessonInfo: title line (fontSize 16, width ~70%) + est-minutes sub-line (fontSize 11, width ~30%) -->

<!-- Hero card skeleton (tree detail loading) — inline inside [treeId].tsx, not a separate component: -->
<!-- - heroCard style exists in [treeId].tsx: borderRadius 20, padding 24, borderWidth 1, marginBottom 24, overflow "hidden" -->
<!-- - Contains: large title block (~60% width, height 28), 3 summary lines, meta row (2 short pills), progress bar track -->
<!-- - Should also have its own shimmer sweep (Animated.loop + LinearGradient) -->

<!-- Header offset pattern — tree detail loading branch uses `paddingTop: headerPaddingTop` not `contentTopOffset` -->
<!-- because headerHeight hasn't measured yet. New loading layout should use a ScrollView with -->
<!-- contentContainerStyle={{ paddingTop: headerPaddingTop + 72 }} (matching estimatedHeaderOffset + 16) -->
<!-- so content sits below the blur header. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TreeCardSkeleton mirroring TreeCard shape</name>
  <files>components/hikmah/TreeCardSkeleton.tsx</files>
  <action>
Create `components/hikmah/TreeCardSkeleton.tsx`. Mirror the animation + theming pattern from `components/references/ReferenceSkeleton.tsx` EXACTLY (do not copy-edit differently):

1. Imports: `React, { useEffect, useRef }`, RN primitives `View, StyleSheet, Animated, Easing, Dimensions`, `LinearGradient` from `expo-linear-gradient`, `Colors` from `@/constants/theme`, `useColorScheme` from `@/hooks/use-color-scheme`.
2. Constants at module top: `const SHIMMER_WIDTH = 200; const CARD_MAX_WIDTH = Dimensions.get("window").width;`.
3. Default export function `TreeCardSkeleton()`:
   - Resolve `colorScheme` + `colors` same as ReferenceSkeleton.
   - `shimmerAnim` via `useRef(new Animated.Value(0)).current`.
   - `useEffect` starts an `Animated.loop(Animated.timing(shimmerAnim, { toValue: 1, duration: 1400, easing: Easing.linear, useNativeDriver: true }))` and returns a cleanup that stops the loop and resets the value.
   - Compute `translateX` via `shimmerAnim.interpolate` with inputRange `[0, 1]` and outputRange `[-SHIMMER_WIDTH, CARD_MAX_WIDTH]`.
   - Compute `highlightColor` using the same light/dark branching (dark = `"rgba(255,255,255,0.06)"`, light = `"rgba(255,255,255,0.65)"`) and `transparentColor = "rgba(255,255,255,0)"`.
4. Render an outer `<View>` with style `[styles.card, { backgroundColor: colors.panel, borderColor: colors.border }]` that contains:
   - `styles.content` View (padding 20, paddingBottom 40) wrapping:
     - Header block: title bar (width "60%", height 20, backgroundColor colors.hoverBg, borderRadius 4, marginBottom 12) and 2 summary lines (widths "90%" and "70%", height 10, marginBottom 6, backgroundColor colors.hoverBg, borderRadius 4).
     - Meta row (flexDirection row, justifyContent space-between, alignItems center, marginTop 12, marginBottom 20): a tag group on the left (flexDirection row, gap 8) with 2 small tag pills (width 52, height 18, borderRadius 12, backgroundColor colors.hoverBg) and a count block on the right (width 60, height 12, borderRadius 4, backgroundColor colors.hoverBg).
     - Button row: a single "View Tree" placeholder (height 44, width "100%", borderRadius 12, borderWidth 1, borderColor colors.border, backgroundColor colors.hoverBg).
   - Progress bar placeholder (absolute bottom, height 4, width "100%", backgroundColor colors.border) — matches TreeCard's `progressContainer`.
   - Shimmer sweep: an `<Animated.View style={[styles.shimmerContainer, { width: SHIMMER_WIDTH, transform: [{ translateX }] }]} pointerEvents="none">` wrapping a `<LinearGradient colors={[transparentColor, highlightColor, transparentColor]} start={{ x: 0, y: 0.5 }} end={{ x: 1, y: 0.5 }} style={styles.shimmerGradient} />`.
5. `const styles = StyleSheet.create({ ... })` at the bottom. The `card` style MUST include `overflow: "hidden"`, `borderRadius: 16`, `borderWidth: 1`, `marginBottom: 16`, matching elevation/shadow from TreeCard (elevation 2, shadowColor "#000", shadowOffset {0,2}, shadowOpacity 0.1, shadowRadius 8). `shimmerContainer` and `shimmerGradient` styles identical to ReferenceSkeleton.

Conventions: 2-space indent, double quotes, trailing commas, StyleSheet.create at bottom, default export. Use `colors.hoverBg` for all block fills (do not hardcode hex). Do NOT use react-native-reanimated — use react-native's built-in Animated to match the canonical pattern.

After writing, run `npm run lint` and confirm zero new warnings for this file. Then commit with message `feat(260416-s86): add TreeCardSkeleton shimmer component` using `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "feat(260416-s86): add TreeCardSkeleton shimmer component" --files components/hikmah/TreeCardSkeleton.tsx`.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
File `components/hikmah/TreeCardSkeleton.tsx` exists, exports default `TreeCardSkeleton` component, uses `Animated.loop` + `LinearGradient` pattern, theme-aware via `Colors[colorScheme]`, lint passes, commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create LessonRowSkeleton mirroring lesson row shape</name>
  <files>components/hikmah/LessonRowSkeleton.tsx</files>
  <action>
Create `components/hikmah/LessonRowSkeleton.tsx` using the same animation + theming pattern as Task 1 (ReferenceSkeleton-style Animated.loop with translating LinearGradient). Differences from TreeCardSkeleton:

1. Same imports, same SHIMMER_WIDTH/CARD_MAX_WIDTH constants, same shimmer animation setup, same highlightColor/transparentColor logic.
2. Render an outer `<View>` styled like `lessonItem` in `app/hikmah/[treeId].tsx`:
   - `backgroundColor: colors.panel, borderColor: colors.border, borderWidth: 1, borderRadius: 16, padding: 16, flexDirection: "row", alignItems: "center", overflow: "hidden", marginBottom: 12` (marginBottom 12 matches the 12-gap lessonsList uses).
3. Inside, render two children:
   - Circular index badge placeholder: width 32, height 32, borderRadius 16, borderWidth 1.5, borderColor colors.border, backgroundColor colors.hoverBg.
   - Info column (marginLeft 12, flex 1): a title block (width "70%", height 14, borderRadius 4, backgroundColor colors.hoverBg, marginBottom 6) and an est-minutes block (width "30%", height 10, borderRadius 4, backgroundColor colors.hoverBg).
4. Append the shimmer sweep (`<Animated.View>` + `<LinearGradient>`) as the last child — absolutely positioned, clipped by `overflow: "hidden"` on the parent. Use the SAME shimmerContainer / shimmerGradient style objects as Task 1 (copy structure exactly).
5. `StyleSheet.create` at the bottom, `const styles = ...`, default export.

Conventions: 2-space indent, double quotes, trailing commas, no reanimated, `@/` path aliases only.

Run `npm run lint` and confirm zero new warnings. Commit with `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "feat(260416-s86): add LessonRowSkeleton shimmer component" --files components/hikmah/LessonRowSkeleton.tsx`.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
File `components/hikmah/LessonRowSkeleton.tsx` exists, exports default `LessonRowSkeleton`, uses Animated.loop + LinearGradient, theme-aware, lint passes, commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire skeletons into both Hikmah loading branches</name>
  <files>app/(tabs)/hikmah.tsx, app/hikmah/[treeId].tsx</files>
  <action>
**Part A — `app/(tabs)/hikmah.tsx` (lines 229-235 area):**

1. Add import near the existing `TreeCard` import: `import TreeCardSkeleton from "@/components/hikmah/TreeCardSkeleton";`.
2. Replace the loading branch JSX (the `loading && !refreshing ?` block currently rendering `<View style={[styles.center, ...]}><ActivityIndicator size="large" color={colors.primary} /><ThemedText>Loading topics...</ThemedText></View>`) with:
   - A `<ScrollView>` (NOT centered) that uses `contentContainerStyle={[styles.scrollContent, { paddingTop: contentTopOffset }]}` so skeletons appear below the blur header and use the same content padding as the real list.
   - Inside the ScrollView: 3 stacked `<TreeCardSkeleton />` instances followed by the existing caption `<ThemedText style={{ marginTop: 8, textAlign: "center", color: colors.textSecondary }}>Loading topics...</ThemedText>` (keep the exact text "Loading topics...").
   - Disable scroll indicator: `showsVerticalScrollIndicator={false}`.
3. You may leave the existing `styles.center` intact — only the loading branch changes. Do not touch the error branch or the main RefreshControl ScrollView branch.
4. Verify `ActivityIndicator` is no longer imported if it's now unused (check the `import` from "react-native" and remove `ActivityIndicator` from the destructured list only if no other usage remains in the file — the file's error branch does not use it). Run lint; if the unused-import rule flags it, remove it.

**Part B — `app/hikmah/[treeId].tsx` (lines 105-114 area):**

1. Add import near the other component imports: `import LessonRowSkeleton from "@/components/hikmah/LessonRowSkeleton";`.
2. Replace the loading branch (the `if (loading || !tree) { return ( ... <ActivityIndicator /> ... ); }` block) with a full-screen layout that preserves the blur header offset:
   - Keep the `<ThemedView style={styles.container}>` + `<Stack.Screen options={{ headerShown: false }} />`.
   - Keep rendering the same `<BlurView>` header as the non-loading branch BUT with a placeholder title block instead of the real `tree.title` — use a simple `<View style={{ width: 140, height: 18, borderRadius: 4, backgroundColor: colors.hoverBg }} />` in place of the ThemedText title. Keep the back button and logo. This prevents the header from appearing/disappearing between loading and loaded states.
   - Below the BlurView, render a `<ScrollView style={styles.scroll} contentContainerStyle={[styles.scrollContent, { paddingTop: headerPaddingTop + 72 + 16 }]} scrollEnabled={false} showsVerticalScrollIndicator={false}>` containing:
     - An inline hero card skeleton styled with `styles.heroCard` + `{ backgroundColor: colors.panel, borderColor: colors.border, overflow: "hidden" }`. Inside: title block (width "70%", height 28, borderRadius 4, backgroundColor colors.hoverBg, marginBottom 12); 3 summary lines (widths "95%", "90%", "60%"; height 12; borderRadius 4; backgroundColor colors.hoverBg; marginBottom 8 each); meta row placeholder (flexDirection row, gap 16, marginTop 8, marginBottom 20) with 2 short pills (width 72, height 14, borderRadius 4, backgroundColor colors.hoverBg); progress bar placeholder (height 6, borderRadius 3, width "100%", backgroundColor colors.border). The hero card should ALSO have its own shimmer sweep — either inline the same Animated.loop + LinearGradient setup or (simpler) extract a small inline `HeroCardSkeletonInner` component at the bottom of the file with the same pattern. Keep implementation compact; it is fine to duplicate the shimmer animation locally rather than create a third skeleton file.
     - A lessons list View with `{ gap: 12 }` containing 4 `<LessonRowSkeleton />` instances.
3. Verify `ActivityIndicator` can be removed from the "react-native" import list if now unused in the file (lint will warn).
4. Use `headerPaddingTop + 72 + 16` for the ScrollView paddingTop — matches the non-loading branch's `estimatedHeaderOffset + 16` pre-measurement fallback (the existing file uses `estimatedHeaderOffset = headerPaddingTop + 72`, and `contentTopOffset = (headerHeight || estimatedHeaderOffset) + 16`). We use the pre-measurement value since headerHeight hasn't been measured yet during loading.

**Finalize:**
- Run `npm run lint`. Fix any issues (unused imports, etc.).
- Commit with `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "feat(260416-s86): wire skeletons into Hikmah loading branches" --files app/(tabs)/hikmah.tsx app/hikmah/[treeId].tsx`.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
`app/(tabs)/hikmah.tsx` loading branch renders 3 `<TreeCardSkeleton />` + "Loading topics..." caption inside a ScrollView with `paddingTop: contentTopOffset`. `app/hikmah/[treeId].tsx` loading branch renders the BlurView header (with placeholder title) + inline hero card skeleton + 4 `<LessonRowSkeleton />` under a ScrollView with `paddingTop: headerPaddingTop + 72 + 16`. Lint passes. Commit landed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human UAT — visual verification in both themes</name>
  <what-built>
Two new skeleton components (`TreeCardSkeleton`, `LessonRowSkeleton`) plus rewired loading branches on the Hikmah tree list screen and tree detail screen. Both use the canonical Animated.loop + LinearGradient shimmer pattern from ReferenceSkeleton.
  </what-built>
  <how-to-verify>
1. Run `npm run start` (or `npm run ios`) to launch the app in Expo Go / simulator.
2. Sign in and navigate to the **Hikmah** tab.
3. Pull to refresh OR cold-launch the tab. While loading, verify:
   - You see ~3 animated skeleton cards stacked below the blur header, not a spinner.
   - Each card has a title bar, 2 summary lines, a tag row + count, a "View Tree" button placeholder, and a thin progress bar at the bottom.
   - A horizontal shimmer gradient sweeps across each card every ~1.4s.
   - The caption "Loading topics..." appears centered below the skeleton stack.
4. Tap into any tree (e.g., first TreeCard). While loading, verify:
   - The blur header with back button + logo is visible immediately (no header pop-in).
   - The header title area shows a placeholder block, not text.
   - Below the header: a hero-card skeleton (large title block, 3 summary lines, meta pills, progress bar) followed by 4 lesson row skeletons (circular badge + title + sub-line).
   - The shimmer animation plays on both the hero card and each lesson row.
   - Nothing overlaps the blur header (respects the same offset as the loaded state).
5. Open Settings and toggle between **Light** and **Dark** theme (or use system toggle). Repeat steps 3-4 and verify:
   - In light mode, the shimmer highlight is a bright white-ish sweep against the panel color.
   - In dark mode, the shimmer highlight is a subtle light sweep (much lower alpha) against the darker panel.
   - Block placeholders use `colors.hoverBg` and look visible but muted in both themes.
6. Verify unchanged flows still work:
   - Error state on Hikmah tab (toggle airplane mode, refresh) still shows the red error + "Try Again".
   - Once loaded, TreeCard and lesson rows render as before — no regressions.
   - Lesson reader (`app/hikmah/lesson/[lessonId].tsx`) is NOT touched and behaves unchanged.

If anything looks off (no shimmer, wrong colors, header overlap, layout jumps when transitioning from loading to loaded), describe the issue. Otherwise type `approved`.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run lint` passes with no new warnings.
- Grep confirms both new files exist and export default: `grep -l "export default function TreeCardSkeleton" components/hikmah/TreeCardSkeleton.tsx` and same for LessonRowSkeleton.
- Grep confirms the wiring: `grep -n "TreeCardSkeleton" app/\(tabs\)/hikmah.tsx` returns import + 3 usages; `grep -n "LessonRowSkeleton" app/hikmah/\[treeId\].tsx` returns import + 4 usages.
- Grep confirms no ActivityIndicator in the two edited files' loading branches: `grep -n "ActivityIndicator" app/\(tabs\)/hikmah.tsx app/hikmah/\[treeId\].tsx` shows at most unused imports (lint will catch).
- `app/hikmah/lesson/[lessonId].tsx` is NOT in `git diff --name-only` — it must stay untouched.
- Human UAT confirms shimmer looks correct on both screens in both light and dark themes.
</verification>

<success_criteria>
- TreeCardSkeleton.tsx and LessonRowSkeleton.tsx committed in `components/hikmah/`.
- `app/(tabs)/hikmah.tsx` loading branch renders 3 TreeCardSkeletons + "Loading topics..." caption.
- `app/hikmah/[treeId].tsx` loading branch renders blur header (with placeholder title) + hero skeleton + 4 LessonRowSkeletons, correctly offset below the header.
- Shimmer animation uses react-native Animated (NOT Reanimated) matching ReferenceSkeleton.
- Theme-aware: different highlight alpha for light vs dark.
- `npm run lint` passes.
- Human UAT approved — looks right in both themes on both screens.
- No changes to `app/hikmah/lesson/[lessonId].tsx`.
</success_criteria>

<output>
After completion, create `.planning/quick/260416-s86-replace-hikmah-loading-spinners-with-ske/260416-s86-01-SUMMARY.md` summarizing: files created, files modified, key patterns reused, and any deviations from plan.
</output>

---
quick_id: 260416-r9d
type: execute
wave: 1
depends_on: []
files_modified:
  - components/onboarding/WelcomeStep.tsx
  - components/onboarding/FeatureStepLayout.tsx
  - components/onboarding/AboutStep.tsx
  - components/onboarding/DoneStep.tsx
  - components/onboarding/AiUsageStep.tsx
autonomous: true
requirements:
  - ONB-HEADER-FIX
must_haves:
  truths:
    - "WelcomeStep 'Deen' title renders with no visible clipping of the tops of glyphs on iOS"
    - "All feature-tour screens (Chatbot, References, Hikmah, Ask Deen, Primers) show complete title glyphs — no top clipping"
    - "AboutStep, DoneStep, and AiUsageStep titles render without top-of-glyph clipping"
    - "Existing layout spacing (gaps, margins, centering) remains visually unchanged — fix only affects glyph rendering room"
    - "PersonalizationStep and AuthStep (already working) are untouched"
  artifacts:
    - path: "components/onboarding/WelcomeStep.tsx"
      provides: "WelcomeStep with paddingTop added to styles.appName"
      contains: "paddingTop"
    - path: "components/onboarding/FeatureStepLayout.tsx"
      provides: "FeatureStepLayout with lineHeight + paddingTop added to styles.title"
      contains: "lineHeight"
    - path: "components/onboarding/AboutStep.tsx"
      provides: "AboutStep with paddingTop added to styles.title"
      contains: "paddingTop"
    - path: "components/onboarding/DoneStep.tsx"
      provides: "DoneStep with paddingTop added to styles.title"
      contains: "paddingTop"
    - path: "components/onboarding/AiUsageStep.tsx"
      provides: "AiUsageStep with paddingTop added to styles.title"
      contains: "paddingTop"
  key_links:
    - from: "WelcomeStep styles.appName"
      to: "Montserrat_700Bold glyph box at fontSize 48"
      via: "lineHeight (60) + paddingTop (6)"
      pattern: "paddingTop:\\s*6"
    - from: "FeatureStepLayout styles.title"
      to: "Montserrat_700Bold glyph box at fontSize 26"
      via: "lineHeight (34) + paddingTop (3)"
      pattern: "lineHeight:\\s*34"
---

<objective>
Fix clipped onboarding screen titles on iOS. The top ~1/3 of Montserrat Bold ascenders (especially the "Deen" glyph in WelcomeStep) is being cut off because several title styles either lack a `lineHeight` entirely (FeatureStepLayout) or lack the `paddingTop` offset that the already-working AuthStep and PersonalizationStep patterns use.

Purpose: Eliminate the visible glyph clipping shown in `assets/images/onboarding_first_page_issue.png` and make all onboarding title styles consistent with the proven pattern (`lineHeight` ≈ 1.3–1.4× `fontSize` + `paddingTop` scaled with size).

Output: Five onboarding component files with title styles updated. No layout, color, animation, or behavior changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Current title style values (from direct file read). Executor must update only these styles. -->
<!-- Proven pattern (from PersonalizationStep + AuthStep): add lineHeight ≈ 1.3–1.4× fontSize and paddingTop scaled with size. -->

components/onboarding/WelcomeStep.tsx — styles.appName (lines 106–114)
CURRENT:
  appName: {
    fontSize: 48,
    lineHeight: 60,
    fontFamily: "Montserrat_700Bold",
    color: "#ffffff",
    letterSpacing: -1,
  },
ISSUE: lineHeight 60 is present but Montserrat_700Bold at 48pt still clips from the TOP in iOS — the existing line box is centered around descenders, leaving no room above ascenders. Needs paddingTop.

components/onboarding/FeatureStepLayout.tsx — styles.title (lines 121–125)
CURRENT:
  title: {
    fontSize: 26,
    fontFamily: "Montserrat_700Bold",
    marginBottom: 6,
  },
ISSUE: No lineHeight at ALL. Used by FeatureChatbotStep, FeatureReferencesStep, FeatureHikmahStep, FeatureAskDeenStep, FeaturePrimersStep. Needs both lineHeight and paddingTop.

components/onboarding/AboutStep.tsx — styles.title (lines 104–109)
CURRENT:
  title: {
    fontSize: 28,
    fontFamily: "Montserrat_700Bold",
    lineHeight: 36,
    marginBottom: 14,
  },
ISSUE: Has lineHeight but no paddingTop. Needs paddingTop.

components/onboarding/DoneStep.tsx — styles.title (lines 77–83)
CURRENT:
  title: {
    fontSize: 34,
    lineHeight: 42,
    fontFamily: "Montserrat_700Bold",
    textAlign: "center",
    marginBottom: 4,
  },
ISSUE: Has lineHeight but no paddingTop. Needs paddingTop.

components/onboarding/AiUsageStep.tsx — styles.title (lines 207–211)
CURRENT:
  title: {
    fontSize: 22,
    fontFamily: "Montserrat_700Bold",
    lineHeight: 30,
  },
ISSUE: Has lineHeight but no paddingTop. Needs paddingTop.

REFERENCE (already working — do NOT modify):
  PersonalizationStep.tsx styles.title: fontSize 26, lineHeight 34, paddingTop 2, marginBottom 6
  AuthStep.tsx         styles.title:    fontSize 26, lineHeight 34, paddingTop 4, marginBottom 6
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add paddingTop (and lineHeight where missing) to onboarding title styles</name>
  <files>
    components/onboarding/WelcomeStep.tsx,
    components/onboarding/FeatureStepLayout.tsx,
    components/onboarding/AboutStep.tsx,
    components/onboarding/DoneStep.tsx,
    components/onboarding/AiUsageStep.tsx
  </files>
  <action>
Edit the `styles.*` title object in each of the five files below using the Edit tool. Touch ONLY the listed style object — do not modify JSX, layout, colors, animations, or any other style. Preserve 2-space indentation, double quotes, trailing commas, and the existing key ordering where possible (add new keys after `fontFamily` / `lineHeight`).

Apply the same proven pattern already used by `PersonalizationStep.tsx` and `AuthStep.tsx`: `lineHeight` ≈ 1.3–1.4× `fontSize` (add if missing), plus `paddingTop` scaled to font size to give Montserrat Bold ascenders room at the top of the line box. Scale rule: ~0.12–0.15× fontSize, rounded to an even integer (2, 4, 6, or 8).

1. **components/onboarding/WelcomeStep.tsx** — `styles.appName` (around line 106–114):
   - Keep: `fontSize: 48`, `lineHeight: 60`, `fontFamily: "Montserrat_700Bold"`, `color: "#ffffff"`, `letterSpacing: -1`.
   - ADD `paddingTop: 6` (48 × ~0.13). This is the PRIORITY fix — the screenshot in `assets/images/onboarding_first_page_issue.png` shows the top of "Deen" clipped, and the existing `lineHeight: 60` alone is insufficient.
   - Preserve the existing comment about iOS line-box clipping; append a short note or leave as-is.

2. **components/onboarding/FeatureStepLayout.tsx** — `styles.title` (around line 121–125):
   - Current: `fontSize: 26`, `fontFamily: "Montserrat_700Bold"`, `marginBottom: 6`.
   - ADD `lineHeight: 34` (matches PersonalizationStep/AuthStep pattern at fontSize 26).
   - ADD `paddingTop: 3` (26 × ~0.12).
   - Final order: `fontSize`, `lineHeight`, `fontFamily`, `paddingTop`, `marginBottom` (or insert new keys consistently; order is cosmetic).

3. **components/onboarding/AboutStep.tsx** — `styles.title` (around line 104–109):
   - Keep: `fontSize: 28`, `fontFamily: "Montserrat_700Bold"`, `lineHeight: 36`, `marginBottom: 14`.
   - ADD `paddingTop: 4` (28 × ~0.14).

4. **components/onboarding/DoneStep.tsx** — `styles.title` (around line 77–83):
   - Keep: `fontSize: 34`, `lineHeight: 42`, `fontFamily: "Montserrat_700Bold"`, `textAlign: "center"`, `marginBottom: 4`.
   - ADD `paddingTop: 4` (34 × ~0.12).

5. **components/onboarding/AiUsageStep.tsx** — `styles.title` (around line 207–211):
   - Keep: `fontSize: 22`, `fontFamily: "Montserrat_700Bold"`, `lineHeight: 30`.
   - ADD `paddingTop: 2` (22 × ~0.10 — smaller since font is smaller).

DO NOT touch: `PersonalizationStep.tsx`, `AuthStep.tsx` (both already correct), any other style key in any file, any JSX, any animation, any color, any spacing/layout property outside the listed title objects. DO NOT reformat surrounding code.
  </action>
  <verify>
    <automated>npm run lint</automated>
    Additionally (human spot-check after lint passes):
    - `grep -n "paddingTop" components/onboarding/WelcomeStep.tsx components/onboarding/FeatureStepLayout.tsx components/onboarding/AboutStep.tsx components/onboarding/DoneStep.tsx components/onboarding/AiUsageStep.tsx` returns one match per file inside the title style object.
    - `grep -n "lineHeight" components/onboarding/FeatureStepLayout.tsx` now returns a match on the `title` style.
  </verify>
  <done>
    - All five files updated; no other lines changed.
    - `npm run lint` exits 0 with no new warnings/errors attributable to these files.
    - TypeScript strict mode still compiles (no type errors — these are `StyleSheet.create` object literals with numeric values, fully typed).
    - Visual spot-check in iOS simulator (Expo Go or dev-client): WelcomeStep "Deen" glyphs fully visible; all five feature-tour titles, AboutStep, DoneStep, AiUsageStep headers render without top clipping.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run lint` — passes with no errors.
2. Start the app (`npm run ios` or `npm run start` → Expo Go) and walk through the onboarding flow from the first screen to "You're all set":
   - **WelcomeStep:** Top of "Deen" (the D ascender especially) is fully visible — compare against `assets/images/onboarding_first_page_issue.png` (should look noticeably different/better).
   - **FeatureChatbotStep, FeatureReferencesStep, FeatureHikmahStep, FeatureAskDeenStep, FeaturePrimersStep:** Each title's ascenders fully visible.
   - **AboutStep:** "Knowledge, grounded in tradition." top clean.
   - **AiUsageStep:** "How your data is used with AI" top clean.
   - **DoneStep:** "You're all set." top clean.
3. Confirm **PersonalizationStep** (interests/knowledge-level screens) and **AuthStep** still render identically to before (they should — they weren't modified).
4. No layout/spacing shifts: other elements on each screen should sit at the same vertical positions they did before (the added `paddingTop` is small and inside the title's own line-box room; any change to sibling spacing should be imperceptible).
</verification>

<success_criteria>
- All five listed files modified; no other files touched.
- Every target title style has an explicit `lineHeight` ≥ ~1.3× `fontSize` AND a `paddingTop` between 2 and 8.
- `npm run lint` passes.
- Onboarding title clipping (especially WelcomeStep "Deen") is visibly resolved on iOS.
- No regression on PersonalizationStep or AuthStep (unchanged).
- No regression in layout, colors, animations, or text content on any onboarding screen.
</success_criteria>

<output>
After completion, create `.planning/quick/260416-r9d-fix-onboarding-screen-headers-top-of-tit/260416-r9d-SUMMARY.md` documenting:
- The five files changed and the exact `paddingTop` / `lineHeight` values applied.
- Confirmation that PersonalizationStep and AuthStep were intentionally left untouched.
- Lint pass confirmation.
- Note whether a visual spot-check was performed and the result.
</output>

---
phase: quick-260519-i4m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.json
  - android/app/build.gradle
  - ios/deenreactnative.xcodeproj/project.pbxproj
autonomous: true
requirements: [QUICK-260519-i4m]
must_haves:
  truths:
    - "app.json reports version 1.0.1"
    - "Android build identifies as versionName 1.0.1 with versionCode 2"
    - "iOS build identifies as MARKETING_VERSION 1.0.1 with CURRENT_PROJECT_VERSION 2 in both Debug and Release configurations"
  artifacts:
    - path: "app.json"
      provides: "Expo user-facing version string"
      contains: '"version": "1.0.1"'
    - path: "android/app/build.gradle"
      provides: "Android Gradle version coordinates"
      contains: 'versionName "1.0.1"'
    - path: "ios/deenreactnative.xcodeproj/project.pbxproj"
      provides: "iOS Xcode marketing + build numbers"
      contains: "MARKETING_VERSION = 1.0.1"
  key_links:
    - from: "app.json version"
      to: "Android versionName + iOS MARKETING_VERSION"
      via: "manual triple-source-of-truth alignment"
      pattern: "1\\.0\\.1"
    - from: "Android versionCode"
      to: "iOS CURRENT_PROJECT_VERSION"
      via: "monotonic store-upload integer (both = 2)"
      pattern: "(versionCode 2|CURRENT_PROJECT_VERSION = 2)"
---

<objective>
Bump the app's user-facing version from 1.0.0 to 1.0.1 across all three platform sources of truth, and increment the monotonic build counters required for store uploads (Android `versionCode` and iOS `CURRENT_PROJECT_VERSION`) from 1 to 2.

Purpose: The user has finished a set of changes and wants to release as 1.0.1. Without bumping `versionCode` / `CURRENT_PROJECT_VERSION`, Google Play and App Store Connect will reject the upload as a duplicate build. Aligning `MARKETING_VERSION` to `1.0.1` (it was previously `1.0`, out of sync with app.json's `1.0.0`) makes the three sources of truth consistent for the first time.

Output: A single atomic commit that touches `app.json`, `android/app/build.gradle`, and `ios/deenreactnative.xcodeproj/project.pbxproj` with the new version coordinates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

# Current verified values (read before this plan was written):
# - app.json line 5:                          "version": "1.0.0"        → "1.0.1"
# - android/app/build.gradle line 95:         versionCode 1             → versionCode 2
# - android/app/build.gradle line 96:         versionName "1.0.0"       → versionName "1.0.1"
# - ios .../project.pbxproj line 345 (Debug): CURRENT_PROJECT_VERSION = 1;   → 2;
# - ios .../project.pbxproj line 357 (Debug): MARKETING_VERSION = 1.0;       → 1.0.1;
# - ios .../project.pbxproj line 381 (Release): CURRENT_PROJECT_VERSION = 1; → 2;
# - ios .../project.pbxproj line 388 (Release): MARKETING_VERSION = 1.0;     → 1.0.1;
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version strings and build numbers across all three platform manifests</name>
  <files>app.json, android/app/build.gradle, ios/deenreactnative.xcodeproj/project.pbxproj</files>
  <action>
Make the following exact edits using the Edit tool. Each edit must be a precise string replacement — do not reformat surrounding lines.

1. **app.json** — single edit:
   - Replace: `"version": "1.0.0"`
   - With:    `"version": "1.0.1"`

2. **android/app/build.gradle** — two edits in the `defaultConfig` block (lines 95–96):
   - Replace: `versionCode 1`
   - With:    `versionCode 2`

   - Replace: `versionName "1.0.0"`
   - With:    `versionName "1.0.1"`

3. **ios/deenreactnative.xcodeproj/project.pbxproj** — four edits. Because `CURRENT_PROJECT_VERSION = 1;` and `MARKETING_VERSION = 1.0;` each appear twice (Debug config ~line 345/357, Release config ~line 381/388), the literal strings are not unique. Use `Edit` with `replace_all: true` for each of these two replacements so both occurrences update in a single call:

   - Replace ALL: `CURRENT_PROJECT_VERSION = 1;`
   - With:        `CURRENT_PROJECT_VERSION = 2;`

   - Replace ALL: `MARKETING_VERSION = 1.0;`
   - With:        `MARKETING_VERSION = 1.0.1;`

   Note: `MARKETING_VERSION = 1.0;` (not `1.0.0`) is the current value — this bump aligns it with the other sources of truth for the first time.

Do NOT touch any other version-like strings (e.g., `IPHONEOS_DEPLOYMENT_TARGET`, `SWIFT_VERSION`, Pod versions, package.json `version`). The scope is exactly the seven line edits above.

Do NOT run `pod install`, `npm install`, or any build. This is a metadata-only change.
  </action>
  <verify>
    <automated>grep -c '"version": "1.0.1"' app.json | grep -q '^1$' && grep -c 'versionCode 2' android/app/build.gradle | grep -q '^1$' && grep -c 'versionName "1.0.1"' android/app/build.gradle | grep -q '^1$' && grep -c 'CURRENT_PROJECT_VERSION = 2;' ios/deenreactnative.xcodeproj/project.pbxproj | grep -q '^2$' && grep -c 'MARKETING_VERSION = 1.0.1;' ios/deenreactnative.xcodeproj/project.pbxproj | grep -q '^2$' && grep -v '^#' app.json | grep -c '"1.0.0"' | grep -q '^0$' && grep -v '^#' android/app/build.gradle | grep -c '"1.0.0"' | grep -q '^0$' && grep -v '^#' ios/deenreactnative.xcodeproj/project.pbxproj | grep -c 'MARKETING_VERSION = 1.0;' | grep -q '^0$' && grep -v '^#' ios/deenreactnative.xcodeproj/project.pbxproj | grep -c 'CURRENT_PROJECT_VERSION = 1;' | grep -q '^0$' && echo OK</automated>
  </verify>
  <done>
- `app.json` contains exactly one occurrence of `"version": "1.0.1"` and zero of `"1.0.0"`.
- `android/app/build.gradle` contains exactly one `versionCode 2` and one `versionName "1.0.1"`, and zero occurrences of `"1.0.0"`.
- `ios/.../project.pbxproj` contains exactly two `CURRENT_PROJECT_VERSION = 2;` and two `MARKETING_VERSION = 1.0.1;`, and zero residual `CURRENT_PROJECT_VERSION = 1;` or `MARKETING_VERSION = 1.0;` lines.
- No other files modified.
  </done>
</task>

</tasks>

<verification>
Final cross-file check (single command):

```bash
grep -n '"version":' app.json
grep -nE 'versionCode|versionName' android/app/build.gradle
grep -nE 'MARKETING_VERSION|CURRENT_PROJECT_VERSION' ios/deenreactnative.xcodeproj/project.pbxproj
```

Expected output:
- `app.json` shows `"version": "1.0.1"`.
- `build.gradle` shows `versionCode 2` and `versionName "1.0.1"`.
- `project.pbxproj` shows two `MARKETING_VERSION = 1.0.1;` and two `CURRENT_PROJECT_VERSION = 2;` (one pair under Debug, one under Release).

Optionally confirm `npm run lint` still passes (the edits do not affect TS/JS, but lint is the only project-wide automated check available — no test suite is configured).
</verification>

<success_criteria>
- All three platform manifests report version `1.0.1` for the user-facing semver.
- Android `versionCode` and iOS `CURRENT_PROJECT_VERSION` both equal `2` (monotonic build counter incremented).
- No stale `1.0.0` / `1.0` / `versionCode 1` / `CURRENT_PROJECT_VERSION = 1` strings remain in the three target files.
- No unrelated files modified.
</success_criteria>

<output>
After completion, create `.planning/quick/260519-i4m-bump-app-version-to-1-0-1/260519-i4m-SUMMARY.md` summarizing the seven edits and confirming the new version coordinates.
</output>

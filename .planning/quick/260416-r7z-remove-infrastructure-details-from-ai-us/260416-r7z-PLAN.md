---
phase: quick-260416-r7z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/onboarding/AiUsageStep.tsx
autonomous: true
requirements:
  - R-01
must_haves:
  truths:
    - "The 'Where it's stored' section no longer names 'Supabase', 'PostgreSQL', or 'Redis'"
    - "The section still communicates that data is on secure servers"
    - "The section still communicates that short-term context is retained ~3.3 hours then auto-deleted"
    - "The section still communicates TLS in transit and at-rest encryption"
    - "The AiUsageStep onboarding screen still renders correctly with no other behavioral changes"
  artifacts:
    - path: "components/onboarding/AiUsageStep.tsx"
      provides: "Updated 'Where it's stored' body string inside SECTIONS array"
      contains: "icon: \"server-outline\""
  key_links:
    - from: "SECTIONS array in components/onboarding/AiUsageStep.tsx"
      to: "rendered card body via SECTIONS.map"
      via: "s.body string consumed by ThemedText at line 122-124"
      pattern: "server-outline"
---

<objective>
Remove low-level infrastructure names (Supabase-hosted PostgreSQL, Redis) from the "Where it's stored" section body in the AI usage disclosure onboarding step. Preserve privacy-relevant facts (secure servers, ~3.3 hour short-term retention with auto-deletion, TLS in transit, at-rest encryption).

Purpose: Implementation details (database/cache vendor names) are unrelated to AI usage disclosure and don't help the user understand privacy. Removing them keeps the disclosure focused on what users actually need to know.

Output: Updated components/onboarding/AiUsageStep.tsx with rephrased body for the "Where it's stored" section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@components/onboarding/AiUsageStep.tsx

<interfaces>
<!-- The SECTIONS array is a local const (not exported). Only the body string -->
<!-- of the entry with icon "server-outline" needs to change. No type changes. -->

Current value (line 52 of components/onboarding/AiUsageStep.tsx):
```
body: "Your conversation history and memory profile are stored on our secure servers (Supabase-hosted PostgreSQL). Short-term context is held in Redis for ~3.3 hours, then deleted automatically. All data is encrypted in transit (TLS) and at rest.",
```

Shape consumed at render time (unchanged):
```typescript
{ icon: "server-outline", title: "Where it's stored", body: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rephrase "Where it's stored" body to remove infrastructure names</name>
  <files>components/onboarding/AiUsageStep.tsx</files>
  <action>
Edit the SECTIONS entry with `icon: "server-outline"` and `title: "Where it's stored"` (currently line 49-53 in components/onboarding/AiUsageStep.tsx).

Replace the current body string:

    "Your conversation history and memory profile are stored on our secure servers (Supabase-hosted PostgreSQL). Short-term context is held in Redis for ~3.3 hours, then deleted automatically. All data is encrypted in transit (TLS) and at rest."

With a rephrased version that:
- Removes the words "Supabase", "Supabase-hosted", "PostgreSQL", and "Redis" entirely
- Keeps the fact that data is stored on secure servers
- Keeps the ~3.3 hour short-term context retention with automatic deletion
- Keeps "encrypted in transit (TLS) and at rest"
- Stays roughly the same length and tone as the other SECTIONS bodies
- Uses natural, user-facing language (avoid terms like "cache", "database")

Suggested copy (you may adjust wording to flow better, but preserve all four facts above):

    "Your conversation history and memory profile are stored on our secure servers. Short-term context used to personalize replies is held temporarily for about 3.3 hours, then deleted automatically. All data is encrypted in transit (TLS) and at rest."

Constraints:
- Do NOT modify any other SECTIONS entry
- Do NOT modify the icon, title, array structure, imports, component logic, or styles
- Preserve the existing 2-space indentation and trailing comma convention used elsewhere in the array (per CLAUDE.md conventions)
- Keep the entry as a single body string (no newlines/bullets added — the other body fields use `\n\n•` only where structured lists exist; this one is prose)
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
- The strings "Supabase", "PostgreSQL", and "Redis" no longer appear anywhere in components/onboarding/AiUsageStep.tsx
- The "Where it's stored" section body still contains "secure servers", "3.3", "TLS", and "at rest"
- `npm run lint` passes with no new warnings/errors attributable to this change
- No other sections, imports, styles, or component logic in AiUsageStep.tsx were modified
  </done>
</task>

</tasks>

<verification>
1. `grep -n "Supabase\|PostgreSQL\|Redis" components/onboarding/AiUsageStep.tsx` returns no matches.
2. `grep -n "secure servers\|3.3\|TLS\|at rest" components/onboarding/AiUsageStep.tsx` still shows all four phrases in the "Where it's stored" body.
3. `npm run lint` exits 0.
4. `git diff components/onboarding/AiUsageStep.tsx` shows only the body string inside the `server-outline` entry changed — no structural, import, or style diffs.
</verification>

<success_criteria>
- Infrastructure vendor/product names (Supabase, PostgreSQL, Redis) removed from the "Where it's stored" disclosure body.
- User-relevant privacy facts preserved: secure servers, ~3.3 hour short-term retention with auto-deletion, TLS in transit, encryption at rest.
- Lint passes.
- No other files or sections touched.
</success_criteria>

<output>
After completion, create `.planning/quick/260416-r7z-remove-infrastructure-details-from-ai-us/260416-r7z-01-SUMMARY.md` following the standard summary template (files changed, before/after snippet of the body string, verification output).
</output>
